<p id="notice"><%= notice %></p>

<h1>Articles</h1>


<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
</div>
<br>
<%= render 'article' %>

<p>
  <td><%= render 'sessions/login_footer' %></td>
  <td><%= link_to 'Create New Article', new_article_path %></td>
  <td><%= link_to 'Users listing', users_path %></td>
  <td><%= link_to 'Home', root_path %></td>
</p>
